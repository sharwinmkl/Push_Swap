# Push_Swap
Push_swap is a coding project where the goal is to sort a stack of numbers using the least possible operations. You work with two stacks (A and B) and a set of allowed moves—like push, swap, rotate, and reverse rotate. The challenge is to write an efficient algorithm that outputs a list of these operations to sort any given numbers correctly. 
